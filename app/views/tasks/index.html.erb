<h1>Task List</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task_path(task) %>
    <% if task.completed %>
      <span>- Completed</span>
    <% else %>
      <span>- Incomplete</span>
    <% end %>
  </li>
<% end %>
</ul>
